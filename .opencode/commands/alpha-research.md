---
name: alpha-research
description: Search, read, and query research papers via the `alpha` CLI when available, with web-backed fallback. Use when the user asks about academic papers, wants to find research on a topic, needs to read a specific paper, ask questions about a paper, inspect a paper's code repository, or manage paper annotations.
---

# Alpha Research CLI

Call the `skill` tool with name `alpha-research`.

User arguments: `$ARGUMENTS`

After the skill loads, execute its workflow using the user arguments. If no paper or query was provided, ask one concise question for the research target before proceeding.
